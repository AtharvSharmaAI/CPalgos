#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t ;
    while(t--){
        int n,p;
        
        cin >> n >> p ;
        vector<char> s(n);
        int mini = INT_MAX;
        int r = n;
        int l = 0;
        int maxi = 0;
        int idx = 0;
        for(int i = 0 ; i<n ; i++){
            cin >> s[i];
            if(s[i]=='#'){
                if( i+1 < p){
                    l = min(p,p-i+1)
                }
            }
        }
        int idx = 
        for(int i = 0 ; i<n ;i++){
            
        }
    }
}